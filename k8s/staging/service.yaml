apiVersion: v1
kind: Service
metadata:
  name: appointment-service-staging-service
spec:
  selector:
    app: appointment-service-deployment-staging
  ports:
    - protocol: TCP
      port: 7070
      targetPort: 7070
  type: NodePort
  